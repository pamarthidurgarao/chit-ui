import { OrderByDateDirective } from './order-by-date.directive';

describe('OrderByDateDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderByDateDirective();
    expect(directive).toBeTruthy();
  });
});
